# apiVersion: v1 
# kind: PersistentVolumeClaim
# metadata:
#   name: ebs-static
# spec:
#   storageClassicName: "" # Empty string must be explicitly set otherwise default storageClass will be set
#   # PV name
#   volumeName: ebs-static
#   accessModes:
#     - ReadWriteOnce
#   resources:
#     requests:
#       storage: 20Gi